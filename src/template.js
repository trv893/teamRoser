// DO NOT TEST THIS
const Employee = require ('../lib/employee');
const Engineer = require ('../lib/engineer');
const Manager = require ('../lib/manager');
const Intern = require ('../lib/intern');

module.exports = funtion(title, data){
    return(
        `
        <h1>${data.titel}</h1>
        `
    )

}